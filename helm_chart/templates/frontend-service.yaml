{{- if .Values.frontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.name }}
  namespace: {{ include "stockwatch.namespace" . }}
  labels:
    {{- include "stockwatch.labels" . | nindent 4 }}
    {{- include "frontend.selectorLabels" . | nindent 4 }}
  {{- with .Values.frontend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.frontend.service.type | default "ClusterIP" }}
  ports:
    # The ingress references this port by name.
    - name: http
      port: {{ .Values.frontend.service.port | default .Values.frontend.containerPort }}
      targetPort: http
      protocol: TCP
      {{- if and .Values.frontend.service.nodePort (eq .Values.frontend.service.type "NodePort") }}
      nodePort: {{ .Values.frontend.service.nodePort }}
      {{- end }}
  selector:
    {{- include "frontend.selectorLabels" . | nindent 4 }}
{{- end }}
